
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@drawable/wood"
    tools:context="edu.pdx.cs.cs554.gomoku.BluetoothActivity"
     >


    <Button
        android:layout_width="200dp"
        android:layout_height="60dp"
        android:text="Host"
        android:id="@+id/button_host"
        android:textSize="30sp"
        android:clickable="false"
        android:layout_above="@+id/button_join"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="52dp"
        android:fontFamily="sans-serif" />
    <Button
        android:layout_width="200dp"
        android:layout_height="60dp"
        android:text="Join"
        android:id="@+id/button_join"
        android:textSize="30sp"
        android:clickable="false"
        android:layout_alignParentBottom="true"
        android:layout_alignLeft="@+id/button_host"
        android:layout_alignStart="@+id/button_host"
        android:layout_marginBottom="61dp"
        android:fontFamily="sans-serif" />

    <RadioGroup

        android:layout_width="200pt"
        android:layout_height="90pt"
        android:orientation="horizontal"
        android:layout_alignBottom="@+id/button_host"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:textColor ="#000000"
        android:id="@+id/radioGroup">
        <RadioButton
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="#000000"
            android:id="@+id/freestyle1"
            android:singleLine="false"
            android:nestedScrollingEnabled="true"
            android:layout_centerHorizontal="true"
            android:allowUndo="true"
            android:background="#91ffffff"
            android:checked="false"
            android:button="@android:color/transparent" />

        <RadioButton
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="#000000"
            android:id="@+id/standard1"
            android:singleLine="false"
            android:nestedScrollingEnabled="true"
            android:layout_centerHorizontal="true"
            android:allowUndo="true"
            android:checked="true"
            android:background="#91ffffff"
            android:button="@android:color/transparent" />

    </RadioGroup>

</RelativeLayout>
